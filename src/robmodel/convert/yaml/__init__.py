'''
Import/export functions for a custom YAML-based robot model format.

The format is defined within this project. It is an attempt to provide a
serialization format for the individual aspects of a robot model
(connectivity, numbering, etc.), separately.
'''
